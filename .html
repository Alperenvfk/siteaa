<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Ultra Özür</title>

<style>
body{
  margin:0;
  font-family:Segoe UI,sans-serif;
  background:linear-gradient(120deg,#ff9a9e,#fad0c4);
  height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
  overflow:hidden;
}

#box{text-align:center;color:#fff}
button{
  padding:15px 30px;
  font-size:1.2em;
  border:none;
  border-radius:10px;
  cursor:pointer;
  margin:10px;
}
#yes{background:#4CAF50;color:#fff}
#no{background:#f44336;color:#fff}

#admin{
  position:fixed;
  top:0;left:0;
  width:100%;height:100%;
  background:#111;
  color:#0f0;
  display:none;
  padding:20px;
  overflow:auto;
  z-index:9999;
}
pre{white-space:pre-wrap}
</style>
</head>

<body>

<div id="box">
  <h1>Özür dilerim reis 😔</h1>
  <button id="yes">Affettim</button>
  <button id="no">Affetmedin</button>
</div>

<div id="admin">
  <h2>👑 GİZLİ ADMIN PANEL</h2>
  <pre id="log"></pre>
  <button onclick="localStorage.clear();location.reload()">🗑️ Tüm kayıtları sil</button>
</div>

<script>
const yes=document.getElementById("yes");
const no=document.getElementById("no");
const admin=document.getElementById("admin");
const log=document.getElementById("log");

function kaydet(durum){
  let isim=prompt("Adın?");
  if(!isim) isim="İsimsiz";
  let liste=JSON.parse(localStorage.getItem("log"))||[];
  liste.push({
    isim,
    durum,
    zaman:new Date().toLocaleString()
  });
  localStorage.setItem("log",JSON.stringify(liste));
}

yes.onclick=()=>{kaydet("AFFETTİ");alert("❤️");};
no.onclick=()=>{kaydet("AFFETMEDİ");alert("😔");};

if(location.hash==="#admin-reis-1907"){
  admin.style.display="block";
  let liste=JSON.parse(localStorage.getItem("log"))||[];
  if(liste.length==0) log.textContent="Kayıt yok";
  else{
    log.textContent="";
    liste.forEach((k,i)=>{
      log.textContent+=`${i+1}) ${k.isim} | ${k.durum} | ${k.zaman}\n`;
    });
  }
}
</script>

</body>
</html>
